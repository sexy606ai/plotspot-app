{
  "name": "plotspot-app",
  "version": "1.0.0",
  "description": "PlotSpot Desktop App",
  "main": "main.js",
  "scripts": {
    "start": "electron .",
    "package": "electron-packager . PlotSpot --platform=linux --arch=arm64 --out=dist --overwrite"
  },
  "devDependencies": {
    "electron": "^32.1.2",
    "electron-packager": "^17.1.2"
  }
}
